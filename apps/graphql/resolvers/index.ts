import { Resolvers } from "@abir-taheer/graphql/generated/graphql";
import { QueryResolvers } from "@abir-taheer/graphql/resolvers/Query";

export const resolvers: Resolvers = {
  Query: QueryResolvers,
};
